#include "main.h"

/**
 * _abs - computes the correct value of an integer
 * @n: the int to check
 * Return: the correct value of int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
